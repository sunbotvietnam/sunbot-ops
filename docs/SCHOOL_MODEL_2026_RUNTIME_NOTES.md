# Runtime Integration Notes

The new v33 school-model layer is intentionally isolated. It must only be loaded after the existing school UI/runtime so it can enrich legacy records without changing core calculations or commercial logic.
