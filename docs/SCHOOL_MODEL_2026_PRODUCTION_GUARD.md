# Production Guard

Do not deploy the v33 model layer until the current Apps Script bundle/include path has been verified. The production URL must remain stable and existing school records must pass smoke tests.
