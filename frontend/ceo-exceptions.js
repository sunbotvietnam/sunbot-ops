(function(){
  function canSee(){const c=(state.boot&&state.boot.can)||{};return !!(c['ceo.view']||c['admin.people']);}
  async function ensure(){if(!canSee()||state.tab!=='outreach')return;const anchor=document.querySelector('#content .ceo-school-summary')||document.querySelector('#content .role-command');if(!anchor||document.getElementById('ceoExceptions'))return;const box=document.createElement('section');box.id='ceoExceptions';box.className='ceo-exceptions';box.innerHTML='<div class="exception-head"><div><strong>CEO Exceptions</strong><span>Những điểm cần can thiệp hoặc ra quyết định</span></div><small>Đang tải…</small></div>';anchor.insertAdjacentElement('afterend',box);try{const r=await call('ceoExceptions','summary',{});paint(box,r);}catch(e){box.querySelector('small').textContent='Chưa tải được: '+e.message;}}
  function paint(box,r){const c=r.counts||{},groups=[['overdue','Quá hạn',c.overdue],['profile_opened_no_followup','Đã mở E-profile nhưng chưa follow-up',c.profile_opened_no_followup],['meeting_no_discovery','Meeting chưa hoàn tất Discovery',c.meeting_no_discovery],['discovery_no_thanks','Discovery chưa gửi phiếu cảm ơn',c.discovery_no_thanks],['pending_proposal','Proposal chờ duyệt',c.pending_proposal],['stale_opportunity','Cơ hội đứng ≥10 ngày',c.stale_opportunity]];box.innerHTML=`<div class="exception-head"><div><strong>CEO Exceptions</strong><span>Ưu tiên ngoại lệ, không đếm activity đơn thuần</span></div><small>${esc(r.generated_at||'')}</small></div><div class="exception-grid">${groups.map(g=>`<button data-ex="${g[0]}"><b>${Number(g[2]||0)}</b><span>${esc(g[1])}</span></button>`).join('')}</div><div id="ceoExceptionDetail"></div>`;box.querySelectorAll('[data-ex]').forEach(b=>b.onclick=()=>detail(r,b.dataset.ex,b.textContent.trim()));}
  function detail(r,key){const host=document.getElementById('ceoExceptionDetail'),items=(r.top&&r.top[key])||[];if(!host)return;if(!items.length){host.innerHTML='<div class="exception-detail muted">Không có trường hợp nào trong nhóm này.</div>';return;}host.innerHTML=`<div class="exception-detail">${items.map(x=>`<article><b>${esc(x.school||'Chưa xác định')}</b><span>${esc(x.note||'')}</span>${x.outreach_id?`<button class="btn ghost" data-open-school="${esc(x.outreach_id)}">Mở hồ sơ</button>`:''}</article>`).join('')}</div>`;host.querySelectorAll('[data-open-school]').forEach(b=>b.onclick=()=>{const row=(state.rows||[]).find(x=>String(x.outreach_id)===String(b.dataset.openSchool));if(row&&window.openSchoolWorkspace)window.openSchoolWorkspace(row);});}
  const obs=new MutationObserver(()=>ensure());obs.observe(document.documentElement,{childList:true,subtree:true});document.addEventListener('DOMContentLoaded',()=>setTimeout(ensure,100));
})();
